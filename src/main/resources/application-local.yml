spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spotwo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: spotwo
    password: spotwo1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true

storage:
  type: minio

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin